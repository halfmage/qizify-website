---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Header from '../../components/Header.astro';
import LanguageSwitcher from '../../components/LanguageSwitcher.astro';
import deContent from '../../content/de.json';

const lang = 'de';
const currentPath = '/de/privacy';
---

<BaseLayout 
	title="Datenschutzerklärung - LearnSlice" 
	description="LearnSlice Datenschutzerklärung"
	lang={lang}
	currentPath={currentPath}
>
	<Header 
		logo={deContent.nav.logo}
		menu={deContent.nav.menu}
		cta={deContent.nav.cta}
		currentLang={lang}
		currentPath={currentPath}
	/>
	
	<main class="container mx-auto px-6 py-16 max-w-4xl">
		<h1 class="text-4xl font-bold mb-8">Datenschutzerklärung</h1>
		
		<div class="prose prose-invert max-w-none">
			<p class="text-gray-400 mb-4">
				Inhalt wird später bereitgestellt.
			</p>
		</div>
	</main>
</BaseLayout>

